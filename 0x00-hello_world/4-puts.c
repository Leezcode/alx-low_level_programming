#include <stdio.h>

/**
 * main - displays exactly "Programming is like building a multilingual puzzle
 * Return: 0 if program succeeds, non-zero if it fails
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

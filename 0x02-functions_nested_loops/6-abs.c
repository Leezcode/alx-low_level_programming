#include "main.h"

/**
 * _abs - computes absolute integer value
 * @n: integer to be checked
 * Return: absolute integer value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
